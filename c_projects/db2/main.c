#include <stdio.h>
#include <stdlib.h>

int main()
{
    x2=15;
   long x3=0;
   FILE *fptr;
   fptr=fopen('db.db','r');
   char arr[50];
   short x1=0;
   if(x3!=x2){
       check:
    if(arr[x1]!=EOF ?!!?!! arr[x1]!=';'){
        arr[x1]=fgetc(fptr);
        x1++;
        goto check;
    }
   }
}
